provider:
  gateway: http://127.0.0.1:8081
cycle_num: 15
faasd_gc_criterion: 120
functions:
  chameleon:
    request_body:
      num_of_rows: 400
      num_of_cols: 400
    max_concurrency: 25
    duration: 2
  dynamic-html:
    request_body:
      username: Tsinghua University
      random_len: 1000
    max_concurrency: 300
    duration: 1
  image-processing:
    request_body:
    max_concurrency: 10
    duration: 5
  image-recognition:
    request_body:
    max_concurrency: 4
    duration: 5
  video-processing:
    request_body:
    max_concurrency: 8
    duration: 5
  image-flip-rotate:
    request_body:
    max_concurrency: 8
    duration: 5

